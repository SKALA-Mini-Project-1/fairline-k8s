apiVersion: v1
kind: Service
metadata:
  name: incident-api
  namespace: fairline
  labels:
    monitoring: enabled
spec:
  selector:
    app: incident-api
  ports:
    - name: http
      port: 18088
      targetPort: 18088
