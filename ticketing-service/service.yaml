apiVersion: v1
kind: Service
metadata:
  name: ticketing-service
  namespace: fairline
  labels:
    monitoring: enabled
spec:
  selector:
    app: ticketing-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
