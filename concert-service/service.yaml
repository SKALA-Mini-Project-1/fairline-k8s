apiVersion: v1
kind: Service
metadata:
  name: concert-service
  namespace: fairline
  labels:
    monitoring: enabled
spec:
  selector:
    app: concert-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
