apiVersion: v1
kind: Service
metadata:
  name: user-auth-service
  namespace: fairline
  labels:
    monitoring: enabled
spec:
  selector:
    app: user-auth-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
