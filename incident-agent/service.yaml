apiVersion: v1
kind: Service
metadata:
  name: incident-agent
  namespace: fairline
  labels:
    monitoring: enabled
spec:
  selector:
    app: incident-agent
  ports:
    - name: http
      port: 18087
      targetPort: 18087
