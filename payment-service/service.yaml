apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: fairline
  labels:
    monitoring: enabled
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
